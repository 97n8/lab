# ui/ — Shared design system

Brand-consistent building blocks shared across product skins (currently inlined in
`apps/web`; to be extracted here).

Planned structure:

- `components/` · `icons/` · `themes/` · `layouts/` · `forms/` · `charts/`

Brand tokens: green `#00453e` · gold `#c59b4b` · cream `#f7f3ea` (see
`apps/web/app/globals.css` and `docs/brand/brand-guide.md`).
