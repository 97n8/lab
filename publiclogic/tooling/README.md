# tooling/ — Repo tooling

Lint configs, codegen, and CI helpers shared across the monorepo.

Planned: shared ESLint/TS config, generators (new capability / new product skin),
and CI scripts. The Turborepo migration (`turbo.json` task graph) lands here when
the workspace count justifies it — today the repo is plain npm workspaces.
