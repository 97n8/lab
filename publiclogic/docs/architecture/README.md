# PublicLogic Architecture

Design docs for how the runtime fits together. These operationalize canon
(`docs/canon`); they don't change it.

| File | What it covers | Status |
|---|---|---|
| `SIGNAL_INTAKE_MCP.md` | How PJ connects to the tools it governs — the Signal Behavior Rule realized over MCP as the tool-agnostic intake transport, feeding the verified spine at the Entry bookend. | Design (spine built; connectors proposed) |
